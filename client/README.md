# Inference client

The planned client will classify or detect one image or a directory of images over
REST or gRPC, support batches and explicit model versions, and display predictions
with client-side timing.

The client is not implemented in step 1.
