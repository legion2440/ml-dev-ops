# Triton model repository

This directory will contain versioned Triton model directories, `config.pbtxt`
files, and generated ONNX or TensorRT artifacts.

Model binaries are not part of step 1. They must later be created by reproducible
preparation commands rather than added manually.
