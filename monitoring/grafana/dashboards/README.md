# Grafana dashboards

`ml-dev-ops.json` is the provisioned Step 7 dashboard. Its five panels deliberately
stay within assignment scope: inference throughput, request rate, average request
latency, GPU utilization, and failed requests.
