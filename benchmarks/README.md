# Benchmarking

This module will run controlled ONNX and TensorRT comparisons using common inputs,
batch sizes, concurrency, warmup, request counts, GPU hardware, and container
versions.

Results must come from measurements; planned claims are not benchmark evidence.
