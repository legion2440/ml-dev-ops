# Deployment

This directory owns container build definitions, lifecycle scripts, Triton runtime
configuration, and deployment health checks.

Step 2 implements the four-service Compose topology and host-side lifecycle. See
`docs/deployment.md` for requirements, commands, completion gates, and limitations.
